import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
  main: {
    fontFamily: "bold",
    fontSize: 20,
    flex: 1,
    width: "100%",
    height: "100%",
  },
});

export default globalStyles;
